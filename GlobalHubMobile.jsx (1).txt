import { useState, useEffect, useRef } from "react";

// ── Design Tokens ─────────────────────────────────────────────────
const C = {
  bg:       "#020408",
  surface:  "#060C14",
  card:     "#0A1220",
  border:   "#0F1E30",
  accent:   "#0FF4C6",
  accentDim:"#0FF4C615",
  blue:     "#2979FF",
  purple:   "#9B6DFF",
  gold:     "#FFB830",
  red:      "#FF3D6B",
  green:    "#00E676",
  text:     "#C8E8F0",
  muted:    "#3A5A70",
};

// ── Mock Data ─────────────────────────────────────────────────────
const stories = [
  { id:1, user:"You", avatar:"👤", hasStory:false, isYou:true },
  { id:2, user:"Sarah", avatar:"👩", hasStory:true, online:true },
  { id:3, user:"Ahmed", avatar:"🧑", hasStory:true, online:false },
  { id:4, user:"Priya", avatar:"👩‍💼", hasStory:true, online:true },
  { id:5, user:"Liu", avatar:"🧑‍💻", hasStory:true, online:false },
  { id:6, user:"James", avatar:"👨‍🎤", hasStory:true, online:true },
];

const feed = [
  {
    id:1, user:"Sarah Chen", handle:"@sarahceo", avatar:"SC", time:"2m",
    verified:true, content:"🚀 Just hit 1M followers on GlobalHub! The future of global business is HERE. Who's building with me?",
    likes:4820, comments:312, shares:891, views:"128K",
    media:true, mediaType:"video", mediaEmoji:"🎬",
  },
  {
    id:2, user:"Ahmed K.", handle:"@ahmedglobal", avatar:"AK", time:"18m",
    verified:false, content:"Our CRM automation on GlobalHub saved us 40hrs/week 🔥 Here's the exact workflow we use for $500K pipeline...",
    likes:2100, comments:189, shares:445, views:"67K",
    media:false,
  },
  {
    id:3, user:"GlobalHub", handle:"@globalhub", avatar:"GH", time:"1h",
    verified:true, content:"🌍 NEW FEATURE DROP: AI-powered live translation in 100+ languages during live streams. Go global instantly!",
    likes:9340, comments:1204, shares:3200, views:"2.4M",
    media:true, mediaType:"image", mediaEmoji:"🌐",
  },
];

const reels = [
  { id:1, emoji:"🎯", title:"$1M in 90 Days", user:"StartupTV", likes:"92K", playing:true },
  { id:2, emoji:"📈", title:"SEO Hack #1 Google", user:"SEOPro", likes:"45K", playing:false },
  { id:3, emoji:"💡", title:"AI Business Ideas 2026", user:"TechMind", likes:"78K", playing:false },
];

const notifications = [
  { id:1, type:"like", icon:"❤️", msg:"Sarah Chen liked your post", time:"1m", read:false },
  { id:2, type:"follow", icon:"👤", msg:"Ahmed K. started following you", time:"5m", read:false },
  { id:3, type:"live", icon:"🔴", msg:"GlobalHub is LIVE now • 45K watching", time:"10m", read:false },
  { id:4, type:"comment", icon:"💬", msg:"Priya replied to your comment", time:"22m", read:true },
  { id:5, type:"mention", icon:"@", msg:"James mentioned you in a post", time:"1h", read:true },
];

const TABS = [
  { id:"home",   icon:"⚡", label:"Home"    },
  { id:"reels",  icon:"▶️",  label:"Reels"   },
  { id:"live",   icon:"🔴", label:"Live"    },
  { id:"search", icon:"🔍", label:"Search"  },
  { id:"profile",icon:"👤", label:"Profile" },
];

// ── Main App ──────────────────────────────────────────────────────
export default function GlobalHubMobile() {
  const [tab, setTab]         = useState("home");
  const [liked, setLiked]     = useState({});
  const [saved, setSaved]     = useState({});
  const [liveViewers, setLV]  = useState(45820);
  const [notifBadge, setNB]   = useState(3);
  const [showNotif, setShowN] = useState(false);
  const [reelIdx, setReelIdx] = useState(0);
  const [searchQ, setSearchQ] = useState("");
  const [uploadOpen, setUpOpen] = useState(false);

  useEffect(() => {
    const t = setInterval(() => setLV(v => v + Math.floor(Math.random()*15-4)), 1800);
    return () => clearInterval(t);
  }, []);

  function toggleLike(id) { setLiked(p => ({...p,[id]:!p[id]})); }
  function toggleSave(id) { setSaved(p => ({...p,[id]:!p[id]})); }

  return (
    <div style={{
      fontFamily:"'Plus Jakarta Sans','Segoe UI',sans-serif",
      background: C.bg,
      color: C.text,
      minHeight: "100vh",
      maxWidth: "420px",
      margin: "0 auto",
      position: "relative",
      overflow: "hidden",
    }}>
      <style>{`
        @import url('https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@300;400;500;600;700;800;900&family=Orbitron:wght@700;900&display=swap');
        *{box-sizing:border-box;margin:0;padding:0}
        body{background:#020408}
        ::-webkit-scrollbar{display:none}
        input,button,textarea{font-family:'Plus Jakarta Sans','Segoe UI',sans-serif}
        .tap{transition:transform .12s ease,opacity .12s ease;cursor:pointer;-webkit-tap-highlight-color:transparent}
        .tap:active{transform:scale(.94);opacity:.8}
        .pulse{animation:pulse 2s infinite}
        @keyframes pulse{0%,100%{opacity:1}50%{opacity:.35}}
        .glow-accent{filter:drop-shadow(0 0 12px ${C.accent}88)}
        .slide-up{animation:slideUp .35s cubic-bezier(.4,0,.2,1)}
        @keyframes slideUp{from{transform:translateY(20px);opacity:0}to{transform:translateY(0);opacity:1}}
        .float{animation:float 3s ease-in-out infinite}
        @keyframes float{0%,100%{transform:translateY(0)}50%{transform:translateY(-6px)}}
        .shimmer{background:linear-gradient(90deg,${C.card},${C.surface},${C.card});background-size:200% 100%;animation:shimmer 2s infinite}
        @keyframes shimmer{0%{background-position:200% 0}100%{background-position:-200% 0}}
        .neon-border{box-shadow:0 0 0 1.5px ${C.accent}44,0 0 20px ${C.accent}22}
        .story-ring{background:conic-gradient(${C.accent},${C.purple},${C.blue},${C.accent})}
        .bottom-blur{background:linear-gradient(to top,${C.bg}FF 40%,${C.bg}00)}
      `}</style>

      {/* ═══ STATUS BAR ═══ */}
      <div style={{
        display:"flex",justifyContent:"space-between",alignItems:"center",
        padding:"10px 18px 6px",fontSize:"11px",fontWeight:"600",color:C.muted,
      }}>
        <span style={{fontFamily:"'Orbitron',monospace",fontSize:"10px",color:C.accent}}>9:41</span>
        <div style={{display:"flex",gap:"6px",alignItems:"center"}}>
          <span>▲▲▲</span><span>WiFi</span><span>🔋</span>
        </div>
      </div>

      {/* ═══ TOP NAV ═══ */}
      <div style={{
        display:"flex",alignItems:"center",justifyContent:"space-between",
        padding:"6px 18px 10px",
      }}>
        {/* Logo */}
        <div style={{display:"flex",alignItems:"center",gap:"7px"}}>
          <div style={{
            width:"32px",height:"32px",borderRadius:"10px",
            background:`linear-gradient(135deg,${C.accent},${C.blue})`,
            display:"flex",alignItems:"center",justifyContent:"center",
            fontSize:"15px",fontWeight:"900",color:"#000",
            boxShadow:`0 0 16px ${C.accent}55`,
          }}>G</div>
          <span style={{
            fontFamily:"'Orbitron',monospace",fontSize:"15px",fontWeight:"900",
            letterSpacing:"-0.5px",
            background:`linear-gradient(90deg,${C.accent},${C.blue})`,
            WebkitBackgroundClip:"text",WebkitTextFillColor:"transparent",
          }}>GLOBALHUB</span>
        </div>

        {/* Right icons */}
        <div style={{display:"flex",gap:"4px",alignItems:"center"}}>
          {/* Upload */}
          <button className="tap" onClick={()=>setUpOpen(true)} style={{
            background:C.accentDim,border:`1px solid ${C.accent}44`,
            borderRadius:"10px",padding:"6px 10px",
            fontSize:"11px",fontWeight:"700",color:C.accent,cursor:"pointer",
            display:"flex",alignItems:"center",gap:"4px",
          }}>+ Post</button>

          {/* Notifications */}
          <div className="tap" style={{position:"relative",padding:"6px",cursor:"pointer"}}
            onClick={()=>{setShowN(!showNotif);setNB(0);}}>
            <span style={{fontSize:"20px"}}>🔔</span>
            {notifBadge>0 && (
              <div style={{
                position:"absolute",top:"3px",right:"3px",
                width:"16px",height:"16px",borderRadius:"50%",
                background:C.red,border:`2px solid ${C.bg}`,
                display:"flex",alignItems:"center",justifyContent:"center",
                fontSize:"8px",fontWeight:"800",color:"#fff",
              }}>{notifBadge}</div>
            )}
          </div>

          {/* Avatar */}
          <div className="tap" style={{
            width:"32px",height:"32px",borderRadius:"50%",
            background:`linear-gradient(135deg,${C.gold},${C.accent})`,
            display:"flex",alignItems:"center",justifyContent:"center",
            fontSize:"13px",fontWeight:"800",color:"#000",cursor:"pointer",
            boxShadow:`0 0 10px ${C.gold}44`,
          }}>U</div>
        </div>
      </div>

      {/* Notifications Dropdown */}
      {showNotif && (
        <div className="slide-up" style={{
          position:"absolute",top:"72px",right:"12px",left:"12px",zIndex:200,
          background:C.card,border:`1px solid ${C.border}`,borderRadius:"16px",
          overflow:"hidden",boxShadow:`0 20px 60px #000A`,
        }}>
          <div style={{padding:"12px 16px",borderBottom:`1px solid ${C.border}`,fontWeight:"700",fontSize:"13px",display:"flex",justifyContent:"space-between"}}>
            Notifications
            <span className="tap" style={{color:C.muted,fontSize:"12px",cursor:"pointer"}}
              onClick={()=>setShowN(false)}>✕</span>
          </div>
          {notifications.map(n=>(
            <div key={n.id} className="tap" style={{
              display:"flex",alignItems:"center",gap:"12px",padding:"12px 16px",
              background:n.read?C.card:`${C.accentDim}`,
              borderBottom:`1px solid ${C.border}`,
            }}>
              <div style={{
                width:"36px",height:"36px",borderRadius:"50%",flexShrink:0,
                background:C.surface,display:"flex",alignItems:"center",justifyContent:"center",
                fontSize:"16px",
              }}>{n.icon}</div>
              <div style={{flex:1}}>
                <div style={{fontSize:"12px",lineHeight:"1.4"}}>{n.msg}</div>
                <div style={{fontSize:"10px",color:C.muted,marginTop:"2px"}}>{n.time} ago</div>
              </div>
              {!n.read && <div style={{width:"7px",height:"7px",borderRadius:"50%",background:C.accent,flexShrink:0}}/>}
            </div>
          ))}
        </div>
      )}

      {/* ═══ SCROLLABLE CONTENT ═══ */}
      <div style={{
        height:"calc(100vh - 130px)",overflowY:"auto",
        paddingBottom:"20px",
      }}>

        {/* ════ HOME TAB ════ */}
        {tab==="home" && (
          <div className="slide-up">
            {/* Stories Row */}
            <div style={{padding:"4px 0 12px",overflowX:"auto",display:"flex",gap:"10px",paddingLeft:"16px",paddingRight:"16px"}}>
              {stories.map(s=>(
                <div key={s.id} className="tap" style={{display:"flex",flexDirection:"column",alignItems:"center",gap:"5px",flexShrink:0,cursor:"pointer"}}>
                  <div style={{
                    width:"58px",height:"58px",borderRadius:"50%",padding:"2.5px",
                    background: s.hasStory ? `conic-gradient(${C.accent},${C.purple},${C.blue},${C.accent})` : C.border,
                  }}>
                    <div style={{
                      width:"100%",height:"100%",borderRadius:"50%",
                      background: s.isYou ? C.surface : `linear-gradient(135deg,${C.card},${C.surface})`,
                      display:"flex",alignItems:"center",justifyContent:"center",
                      fontSize:"22px",border:`2px solid ${C.bg}`,position:"relative",
                    }}>
                      {s.isYou ? <span style={{fontSize:"20px",color:C.accent}}>+</span> : s.avatar}
                      {s.online && !s.isYou && (
                        <div style={{
                          position:"absolute",bottom:"1px",right:"1px",
                          width:"12px",height:"12px",borderRadius:"50%",
                          background:C.green,border:`2px solid ${C.bg}`,
                        }}/>
                      )}
                    </div>
                  </div>
                  <span style={{fontSize:"10px",fontWeight:"600",color:C.muted,maxWidth:"58px",overflow:"hidden",textOverflow:"ellipsis",whiteSpace:"nowrap",textAlign:"center"}}>
                    {s.isYou?"Your Story":s.user}
                  </span>
                </div>
              ))}
            </div>

            {/* Live Banner */}
            <div className="tap neon-border" style={{
              margin:"0 14px 14px",borderRadius:"16px",overflow:"hidden",
              background:`linear-gradient(135deg,#0A0015,#001020)`,
              position:"relative",cursor:"pointer",
            }}>
              <div style={{
                position:"absolute",top:0,left:0,right:0,bottom:0,
                background:`radial-gradient(ellipse at 30% 50%,${C.accent}15,transparent 60%)`,
              }}/>
              <div style={{padding:"14px 16px",display:"flex",alignItems:"center",gap:"12px",position:"relative"}}>
                <div style={{
                  width:"44px",height:"44px",borderRadius:"12px",
                  background:`linear-gradient(135deg,${C.red},#FF6B35)`,
                  display:"flex",alignItems:"center",justifyContent:"center",
                  fontSize:"20px",flexShrink:0,
                  boxShadow:`0 0 20px ${C.red}55`,
                }}>🌍</div>
                <div style={{flex:1}}>
                  <div style={{display:"flex",alignItems:"center",gap:"6px",marginBottom:"3px"}}>
                    <div style={{
                      background:C.red,borderRadius:"4px",padding:"1px 6px",
                      fontSize:"9px",fontWeight:"800",color:"#fff",letterSpacing:"1px",
                      display:"flex",alignItems:"center",gap:"3px",
                    }}>
                      <span className="pulse">●</span> LIVE
                    </div>
                    <span style={{fontSize:"11px",color:C.muted}}>Global Summit 2026</span>
                  </div>
                  <div style={{fontSize:"13px",fontWeight:"700"}}>Entrepreneur Summit Live</div>
                  <div style={{fontSize:"11px",color:C.accent,marginTop:"2px"}}>
                    👁 {liveViewers.toLocaleString()} watching
                  </div>
                </div>
                <button style={{
                  background:C.red,border:"none",color:"#fff",
                  borderRadius:"20px",padding:"7px 14px",fontSize:"11px",
                  fontWeight:"800",cursor:"pointer",flexShrink:0,
                }}>Join ▶</button>
              </div>
            </div>

            {/* Feed */}
            {feed.map(post=>(
              <div key={post.id} className="slide-up" style={{
                margin:"0 0 8px",
                background:C.surface,borderTop:`1px solid ${C.border}`,borderBottom:`1px solid ${C.border}`,
              }}>
                {/* Post Header */}
                <div style={{display:"flex",alignItems:"center",gap:"10px",padding:"12px 14px 8px"}}>
                  <div style={{
                    width:"40px",height:"40px",borderRadius:"50%",flexShrink:0,
                    background:`linear-gradient(135deg,${C.purple},${C.blue})`,
                    display:"flex",alignItems:"center",justifyContent:"center",
                    fontSize:"13px",fontWeight:"800",
                  }}>{post.avatar}</div>
                  <div style={{flex:1}}>
                    <div style={{display:"flex",alignItems:"center",gap:"4px"}}>
                      <span style={{fontWeight:"700",fontSize:"13px"}}>{post.user}</span>
                      {post.verified && <span style={{color:C.accent,fontSize:"12px"}}>✓</span>}
                    </div>
                    <div style={{fontSize:"11px",color:C.muted}}>{post.handle} · {post.time} ago</div>
                  </div>
                  <button style={{
                    background:C.accentDim,border:`1px solid ${C.accent}44`,
                    color:C.accent,borderRadius:"16px",padding:"5px 12px",
                    fontSize:"11px",fontWeight:"700",cursor:"pointer",
                  }}>Follow</button>
                </div>

                {/* Post Content */}
                <div style={{padding:"0 14px 10px",fontSize:"13px",lineHeight:"1.6"}}>{post.content}</div>

                {/* Media */}
                {post.media && (
                  <div style={{
                    margin:"0 14px 10px",borderRadius:"12px",overflow:"hidden",
                    background:`linear-gradient(135deg,#0A0A1A,#1A0A2A)`,
                    aspectRatio:"16/9",display:"flex",alignItems:"center",
                    justifyContent:"center",flexDirection:"column",gap:"8px",
                    border:`1px solid ${C.border}`,
                  }}>
                    <span style={{fontSize:"40px"}} className="float">{post.mediaEmoji}</span>
                    {post.mediaType==="video" && (
                      <div style={{
                        background:`${C.accent}22`,border:`1px solid ${C.accent}44`,
                        borderRadius:"20px",padding:"5px 14px",
                        fontSize:"11px",color:C.accent,fontWeight:"600",
                        display:"flex",alignItems:"center",gap:"5px",
                      }}>▶ Play Video · {post.views} views</div>
                    )}
                  </div>
                )}

                {/* Actions */}
                <div style={{
                  display:"flex",alignItems:"center",gap:"2px",
                  padding:"4px 10px 12px",borderTop:`1px solid ${C.border}33`,marginTop:"4px",
                }}>
                  {[
                    { icon: liked[post.id]?"❤️":"🤍", count: post.likes+(liked[post.id]?1:0), action:()=>toggleLike(post.id), active: liked[post.id] },
                    { icon:"💬", count:post.comments, action:()=>{} },
                    { icon:"🔁", count:post.shares, action:()=>{} },
                  ].map((a,i)=>(
                    <button key={i} className="tap" onClick={a.action} style={{
                      flex:1,background:"transparent",border:"none",
                      color:a.active?C.red:C.muted,cursor:"pointer",
                      display:"flex",alignItems:"center",justifyContent:"center",
                      gap:"5px",padding:"8px 4px",fontSize:"12px",fontWeight:"600",
                    }}>
                      <span style={{fontSize:"16px"}}>{a.icon}</span>
                      <span>{a.count>=1000?`${(a.count/1000).toFixed(1)}K`:a.count}</span>
                    </button>
                  ))}
                  <button className="tap" onClick={()=>toggleSave(post.id)} style={{
                    background:"transparent",border:"none",
                    color:saved[post.id]?C.gold:C.muted,cursor:"pointer",
                    padding:"8px",fontSize:"18px",
                  }}>{saved[post.id]?"🔖":"🏷"}</button>
                  <button className="tap" style={{
                    background:"transparent",border:"none",
                    color:C.muted,cursor:"pointer",padding:"8px",fontSize:"18px",
                  }}>↗</button>
                </div>
              </div>
            ))}
          </div>
        )}

        {/* ════ REELS TAB ════ */}
        {tab==="reels" && (
          <div style={{height:"calc(100vh - 130px)",position:"relative",overflow:"hidden"}}>
            {/* Full screen reel */}
            <div style={{
              height:"100%",
              background:`linear-gradient(180deg,#050010 0%,#001020 50%,#000508 100%)`,
              display:"flex",alignItems:"center",justifyContent:"center",
              position:"relative",
            }}>
              {/* Content */}
              <div style={{textAlign:"center"}}>
                <div style={{fontSize:"72px",marginBottom:"16px"}} className="float">
                  {reels[reelIdx].emoji}
                </div>
                <div style={{
                  fontFamily:"'Orbitron',monospace",fontSize:"18px",
                  fontWeight:"900",marginBottom:"8px",
                  background:`linear-gradient(90deg,${C.accent},${C.blue})`,
                  WebkitBackgroundClip:"text",WebkitTextFillColor:"transparent",
                }}>{reels[reelIdx].title}</div>
                <div style={{color:C.muted,fontSize:"13px"}}>{reels[reelIdx].user}</div>
              </div>

              {/* Right Actions */}
              <div style={{
                position:"absolute",right:"16px",bottom:"120px",
                display:"flex",flexDirection:"column",gap:"20px",alignItems:"center",
              }}>
                {[
                  {icon:"❤️",count:reels[reelIdx].likes,color:C.red},
                  {icon:"💬",count:"4.2K",color:C.blue},
                  {icon:"↗",count:"Share",color:C.accent},
                ].map((a,i)=>(
                  <div key={i} className="tap" style={{textAlign:"center",cursor:"pointer"}}>
                    <div style={{
                      width:"44px",height:"44px",borderRadius:"50%",
                      background:`${C.bg}AA`,border:`1px solid ${C.border}`,
                      backdropFilter:"blur(10px)",
                      display:"flex",alignItems:"center",justifyContent:"center",fontSize:"20px",
                    }}>{a.icon}</div>
                    <div style={{fontSize:"10px",fontWeight:"700",marginTop:"4px",color:C.muted}}>{a.count}</div>
                  </div>
                ))}
              </div>

              {/* Bottom Info */}
              <div style={{
                position:"absolute",bottom:"20px",left:"16px",right:"70px",
              }}>
                <div style={{display:"flex",alignItems:"center",gap:"8px",marginBottom:"8px"}}>
                  <div style={{
                    width:"32px",height:"32px",borderRadius:"50%",
                    background:`linear-gradient(135deg,${C.purple},${C.blue})`,
                    display:"flex",alignItems:"center",justifyContent:"center",
                    fontSize:"14px",fontWeight:"700",
                  }}>🎬</div>
                  <span style={{fontWeight:"700",fontSize:"14px"}}>{reels[reelIdx].user}</span>
                  <button style={{
                    background:"transparent",border:`1px solid #fff`,
                    color:"#fff",borderRadius:"12px",padding:"3px 10px",
                    fontSize:"11px",fontWeight:"700",cursor:"pointer",
                  }}>Follow</button>
                </div>
                <div style={{fontSize:"13px",lineHeight:"1.4",color:"rgba(255,255,255,.85)"}}>
                  {reels[reelIdx].title} 🔥 #globalhub #business #viral
                </div>
              </div>

              {/* Swipe Indicators */}
              <div style={{
                position:"absolute",right:"6px",top:"50%",transform:"translateY(-50%)",
                display:"flex",flexDirection:"column",gap:"6px",
              }}>
                {reels.map((_,i)=>(
                  <div key={i} className="tap" onClick={()=>setReelIdx(i)} style={{
                    width:"3px",height: i===reelIdx ? "24px" : "12px",
                    borderRadius:"2px",
                    background: i===reelIdx ? C.accent : C.border,
                    transition:"all .3s",cursor:"pointer",
                  }}/>
                ))}
              </div>
            </div>
          </div>
        )}

        {/* ════ LIVE TAB ════ */}
        {tab==="live" && (
          <div className="slide-up" style={{padding:"14px"}}>
            {/* Main Live */}
            <div style={{
              borderRadius:"16px",overflow:"hidden",marginBottom:"14px",
              border:`1.5px solid ${C.red}66`,
              boxShadow:`0 0 30px ${C.red}22`,
            }}>
              <div style={{
                aspectRatio:"16/9",background:"linear-gradient(135deg,#0A0005,#05001A)",
                display:"flex",alignItems:"center",justifyContent:"center",
                flexDirection:"column",gap:"10px",position:"relative",
              }}>
                <div style={{
                  position:"absolute",top:"10px",left:"10px",
                  background:C.red,borderRadius:"6px",padding:"3px 8px",
                  fontSize:"10px",fontWeight:"800",
                  display:"flex",alignItems:"center",gap:"4px",
                }}>
                  <span className="pulse">●</span> LIVE
                </div>
                <div style={{
                  position:"absolute",top:"10px",right:"10px",
                  background:"#000A",borderRadius:"8px",padding:"3px 8px",
                  fontSize:"10px",color:C.text,
                }}>👁 {liveViewers.toLocaleString()}</div>
                <span style={{fontSize:"50px"}} className="float">🌍</span>
                <span style={{fontWeight:"800",fontSize:"15px"}}>Global Entrepreneur Summit</span>
                <button style={{
                  background:`linear-gradient(135deg,${C.red},#FF6B35)`,
                  border:"none",color:"#fff",borderRadius:"24px",
                  padding:"10px 28px",fontSize:"13px",fontWeight:"800",cursor:"pointer",
                }}>▶ Watch Now</button>
              </div>
            </div>

            <div style={{fontWeight:"700",fontSize:"14px",marginBottom:"10px"}}>🔴 More Live Streams</div>
            {[
              {title:"AI Business Workshop",user:"TechHub",viewers:"12K",emoji:"🤖"},
              {title:"Marketing Masterclass",user:"GrowthPro",viewers:"8.4K",emoji:"📈"},
              {title:"Startup Funding Q&A",user:"VCTalk",viewers:"5.1K",emoji:"💰"},
            ].map((s,i)=>(
              <div key={i} className="tap" style={{
                display:"flex",gap:"12px",alignItems:"center",
                background:C.card,borderRadius:"12px",padding:"12px",marginBottom:"8px",
                border:`1px solid ${C.border}`,
              }}>
                <div style={{
                  width:"60px",height:"44px",borderRadius:"8px",flexShrink:0,
                  background:"linear-gradient(135deg,#0A0A1A,#1A0A2A)",
                  display:"flex",alignItems:"center",justifyContent:"center",fontSize:"22px",
                }}>{s.emoji}</div>
                <div style={{flex:1}}>
                  <div style={{fontWeight:"600",fontSize:"13px"}}>{s.title}</div>
                  <div style={{fontSize:"11px",color:C.muted,marginTop:"2px"}}>{s.user} · 👁 {s.viewers}</div>
                </div>
                <div style={{
                  background:`${C.red}22`,border:`1px solid ${C.red}44`,
                  borderRadius:"8px",padding:"3px 8px",fontSize:"10px",
                  color:C.red,fontWeight:"700",
                  display:"flex",alignItems:"center",gap:"3px",flexShrink:0,
                }}>
                  <span className="pulse">●</span>LIVE
                </div>
              </div>
            ))}
          </div>
        )}

        {/* ════ SEARCH TAB ════ */}
        {tab==="search" && (
          <div className="slide-up" style={{padding:"10px 14px"}}>
            <div style={{
              display:"flex",background:C.card,border:`1px solid ${C.border}`,
              borderRadius:"14px",marginBottom:"16px",overflow:"hidden",
            }}>
              <span style={{padding:"12px 14px",fontSize:"16px"}}>🔍</span>
              <input value={searchQ} onChange={e=>setSearchQ(e.target.value)}
                placeholder="Search GlobalHub…"
                style={{
                  flex:1,background:"transparent",border:"none",outline:"none",
                  color:C.text,fontSize:"14px",padding:"12px 0",
                }}/>
            </div>

            <div style={{fontWeight:"700",fontSize:"13px",marginBottom:"10px",color:C.muted,letterSpacing:"1px"}}>TRENDING</div>
            {[
              {tag:"#GlobalBusiness",posts:"2.4M posts",emoji:"🌍"},
              {tag:"#AIMarketing2026",posts:"890K posts",emoji:"🤖"},
              {tag:"#StartupLife",posts:"5.1M posts",emoji:"🚀"},
              {tag:"#YouTubeAlternative",posts:"340K posts",emoji:"🎬"},
              {tag:"#CRMAutomation",posts:"120K posts",emoji:"⚙️"},
            ].map((t,i)=>(
              <div key={i} className="tap" style={{
                display:"flex",alignItems:"center",gap:"12px",
                padding:"12px 0",borderBottom:`1px solid ${C.border}`,cursor:"pointer",
              }}>
                <div style={{
                  width:"44px",height:"44px",borderRadius:"12px",
                  background:C.card,display:"flex",alignItems:"center",
                  justifyContent:"center",fontSize:"22px",flexShrink:0,
                  border:`1px solid ${C.border}`,
                }}>{t.emoji}</div>
                <div>
                  <div style={{fontWeight:"700",fontSize:"14px"}}>{t.tag}</div>
                  <div style={{fontSize:"11px",color:C.muted,marginTop:"2px"}}>{t.posts}</div>
                </div>
                <span style={{marginLeft:"auto",color:C.muted,fontSize:"16px"}}>›</span>
              </div>
            ))}
          </div>
        )}

        {/* ════ PROFILE TAB ════ */}
        {tab==="profile" && (
          <div className="slide-up">
            {/* Cover */}
            <div style={{
              height:"120px",
              background:`linear-gradient(135deg,${C.accentDim},#000820,${C.purple}22)`,
              position:"relative",display:"flex",alignItems:"flex-end",
            }}>
              <div style={{
                position:"absolute",inset:0,
                backgroundImage:`repeating-linear-gradient(0deg,transparent,transparent 20px,${C.border}44 20px,${C.border}44 21px),repeating-linear-gradient(90deg,transparent,transparent 20px,${C.border}44 20px,${C.border}44 21px)`,
              }}/>
            </div>

            {/* Avatar + Info */}
            <div style={{padding:"0 16px",marginBottom:"16px"}}>
              <div style={{
                width:"76px",height:"76px",borderRadius:"50%",
                background:`linear-gradient(135deg,${C.gold},${C.accent})`,
                display:"flex",alignItems:"center",justifyContent:"center",
                fontSize:"28px",fontWeight:"800",color:"#000",
                border:`3px solid ${C.bg}`,marginTop:"-38px",
                boxShadow:`0 0 24px ${C.gold}55`,
              }}>U</div>
              <div style={{marginTop:"10px"}}>
                <div style={{fontWeight:"800",fontSize:"18px"}}>Your Name</div>
                <div style={{color:C.muted,fontSize:"13px",marginTop:"2px"}}>@yourhandle · 🌍 Global Entrepreneur</div>
              </div>
              <div style={{display:"flex",gap:"20px",margin:"14px 0"}}>
                {[["1.2K","Posts"],["45K","Followers"],["890","Following"]].map(([n,l],i)=>(
                  <div key={i} style={{textAlign:"center"}}>
                    <div style={{fontWeight:"800",fontSize:"16px",fontFamily:"'Orbitron',monospace",color:C.accent}}>{n}</div>
                    <div style={{fontSize:"11px",color:C.muted}}>{l}</div>
                  </div>
                ))}
              </div>
              <button style={{
                width:"100%",background:`linear-gradient(135deg,${C.accent},${C.blue})`,
                border:"none",color:"#000",borderRadius:"12px",padding:"11px",
                fontSize:"13px",fontWeight:"800",cursor:"pointer",
              }}>Edit Profile</button>
            </div>

            {/* Stats */}
            <div style={{display:"grid",gridTemplateColumns:"1fr 1fr",gap:"8px",padding:"0 14px"}}>
              {[
                {icon:"🎬",label:"Videos",val:"48"},
                {icon:"👁",label:"Total Views",val:"2.4M"},
                {icon:"❤️",label:"Total Likes",val:"89K"},
                {icon:"💼",label:"CRM Deals",val:"$280K"},
              ].map((s,i)=>(
                <div key={i} style={{
                  background:C.card,border:`1px solid ${C.border}`,
                  borderRadius:"14px",padding:"14px",textAlign:"center",
                }}>
                  <div style={{fontSize:"22px",marginBottom:"4px"}}>{s.icon}</div>
                  <div style={{fontWeight:"800",fontSize:"18px",color:C.accent,fontFamily:"'Orbitron',monospace"}}>{s.val}</div>
                  <div style={{fontSize:"11px",color:C.muted,marginTop:"2px"}}>{s.label}</div>
                </div>
              ))}
            </div>
          </div>
        )}
      </div>

      {/* ═══ BOTTOM NAV ═══ */}
      <div style={{
        position:"fixed",bottom:0,left:"50%",transform:"translateX(-50%)",
        width:"100%",maxWidth:"420px",
        background:`${C.bg}F8`,backdropFilter:"blur(20px)",
        borderTop:`1px solid ${C.border}`,
        display:"flex",padding:"8px 0 16px",zIndex:100,
      }}>
        {TABS.map(t=>(
          <button key={t.id} className="tap" onClick={()=>setTab(t.id)} style={{
            flex:1,background:"none",border:"none",cursor:"pointer",
            display:"flex",flexDirection:"column",alignItems:"center",gap:"3px",
            padding:"4px 0",
          }}>
            <span style={{
              fontSize:"20px",
              filter: tab===t.id?`drop-shadow(0 0 8px ${C.accent})`:"none",
              transition:"filter .2s",
            }}>{t.icon}</span>
            <span style={{
              fontSize:"9px",fontWeight:tab===t.id?"800":"500",
              color:tab===t.id?C.accent:C.muted,
              transition:"color .2s",
              fontFamily:"'Plus Jakarta Sans',sans-serif",
            }}>{t.label}</span>
            {tab===t.id && (
              <div style={{
                width:"4px",height:"4px",borderRadius:"50%",
                background:C.accent,boxShadow:`0 0 6px ${C.accent}`,
              }}/>
            )}
          </button>
        ))}
      </div>

      {/* ═══ UPLOAD MODAL ═══ */}
      {uploadOpen && (
        <div style={{
          position:"fixed",inset:0,zIndex:300,
          background:"#000C",backdropFilter:"blur(8px)",
          display:"flex",alignItems:"flex-end",
        }} onClick={()=>setUpOpen(false)}>
          <div className="slide-up" style={{
            width:"100%",maxWidth:"420px",margin:"0 auto",
            background:C.card,borderRadius:"24px 24px 0 0",
            border:`1px solid ${C.border}`,
            padding:"20px",
          }} onClick={e=>e.stopPropagation()}>
            <div style={{
              width:"36px",height:"4px",borderRadius:"2px",
              background:C.border,margin:"0 auto 20px",
            }}/>
            <div style={{fontWeight:"800",fontSize:"17px",marginBottom:"20px",textAlign:"center"}}>Create Post</div>
            {[
              {icon:"🎬",title:"Upload Video",desc:"Share a video with the world"},
              {icon:"🔴",title:"Go Live",desc:"Start a live stream now"},
              {icon:"📸",title:"Photo / Story",desc:"Share an image or story"},
              {icon:"📊",title:"Create Poll",desc:"Ask your community"},
            ].map((o,i)=>(
              <div key={i} className="tap" style={{
                display:"flex",gap:"14px",alignItems:"center",
                background:C.surface,border:`1px solid ${C.border}`,
                borderRadius:"14px",padding:"14px",marginBottom:"8px",cursor:"pointer",
              }}>
                <div style={{
                  width:"44px",height:"44px",borderRadius:"12px",
                  background:C.card,display:"flex",alignItems:"center",
                  justifyContent:"center",fontSize:"22px",flexShrink:0,
                }}>{o.icon}</div>
                <div>
                  <div style={{fontWeight:"700",fontSize:"14px"}}>{o.title}</div>
                  <div style={{fontSize:"12px",color:C.muted,marginTop:"2px"}}>{o.desc}</div>
                </div>
                <span style={{marginLeft:"auto",color:C.muted}}>›</span>
              </div>
            ))}
            <button onClick={()=>setUpOpen(false)} style={{
              width:"100%",background:C.surface,border:`1px solid ${C.border}`,
              color:C.muted,borderRadius:"12px",padding:"12px",
              fontSize:"14px",cursor:"pointer",marginTop:"4px",
            }}>Cancel</button>
          </div>
        </div>
      )}
    </div>
  );
}
